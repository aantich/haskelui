name: haskelui-property-spike
version: 0.1.0.0
github: aantich/haskelui
license: BSD-3-Clause
author: HaskeLUI contributors
maintainer: HaskeLUI contributors
copyright: 2026 HaskeLUI contributors

extra-source-files:
  - README.md

dependencies:
  - base >= 4.20.2 && < 4.21
  - containers
  - generic-lens
  - text

ghc-options:
  - -Wall
  - -Wcompat

library:
  source-dirs: src

tests:
  property-api-test:
    main: Spec.hs
    source-dirs: test
    dependencies:
      - haskelui-property-spike
